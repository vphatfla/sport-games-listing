services:
  database:
    build:
      context: ./database
    ports:
      - "27017:27017"
    volumes:
      - dbdata:/data/db
    restart: unless-stopped

  backend:
    build:za
      context: ./back-end
    ports:
      - "3000:3000"
    depends_on:
      - database
    restart: unless-stopped

volumes:
  dbdata:
    
